# 🔧 Исправление ошибки 404 на Vercel

## Проблема

При открытии сайта на Vercel появляется ошибка:
```
404: NOT_FOUND
Code: `NOT_FOUND`
```

## Причина

Vercel пытается развернуть корень проекта, а Next.js приложение находится в папке `web/`.

## ✅ Решение

### Способ 1: Через Web Interface (рекомендуется)

1. **Зайдите на Vercel Dashboard**
   https://vercel.com/dashboard

2. **Откройте ваш проект** (smi-los1)

3. **Перейдите в Settings**
   Settings → General

4. **Найдите секцию "Build & Development Settings"**

5. **Измените Root Directory:**
   - Нажмите **Edit** рядом с "Root Directory"
   - Введите: `web`
   - Нажмите **Save**

6. **Vercel автоматически запустит Redeploy**
   - Или вручную: Deployments → ⋯ → Redeploy

7. **Дождитесь завершения деплоя** (1-2 минуты)

8. **Откройте сайт** - должна появиться страница входа `/login`

---

### Способ 2: Через vercel.json (альтернатива)

Обновите файл `web/vercel.json`:

```json
{
  "buildCommand": "cd .. && cd web && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
```

Но **Способ 1 проще и надежнее!**

---

## 🔐 После исправления - добавьте переменные окружения

После успешного деплоя добавьте переменные:

**Settings → Environment Variables**

Добавьте:

```
NEXTAUTH_URL=https://smi-los1.vercel.app
NEXTAUTH_SECRET=<generate at https://generate-secret.vercel.app/32>
ADMIN_USERNAME=admin
ADMIN_PASSWORD=ваш_надежный_пароль
API_BASE_URL=http://localhost:3001
```

Затем **Redeploy** еще раз.

---

## ✅ Проверка

После деплоя:

1. Откройте: https://smi-los1.vercel.app
2. Должна открыться страница `/login`
3. Введите учетные данные
4. Должен открыться Dashboard

---

## 📊 Правильные настройки Vercel

| Параметр | Значение |
|----------|----------|
| **Framework Preset** | Next.js |
| **Root Directory** | `web` |
| **Build Command** | `npm run build` |
| **Output Directory** | `.next` |
| **Install Command** | `npm install` |

---

## 🐛 Если все еще не работает

### 1. Проверьте логи деплоя

Deployments → Нажмите на последний деплой → Build Logs

Ищите ошибки в логах.

### 2. Убедитесь что package.json существует

В папке `web/` должен быть файл `package.json`

### 3. Проверьте Node.js версию

Vercel использует Node.js 18.x по умолчанию.

Можно указать версию в `web/package.json`:

```json
{
  "engines": {
    "node": "18.x"
  }
}
```

### 4. Очистите кэш и пересоберите

Deployments → ⋯ → Redeploy → ✅ "Clear cache and redeploy"

---

## 🚀 Полный процесс настройки

1. ✅ Установить Root Directory: `web`
2. ✅ Добавить переменные окружения
3. ✅ Redeploy проект
4. ✅ Проверить страницу `/login`
5. ✅ Войти с учетными данными
6. ✅ Проверить Dashboard

---

## 📝 Скриншоты (для справки)

### Root Directory должен быть:
```
Root Directory: web
```

### Build Command:
```
npm run build
```

### Framework:
```
Next.js (автоматически)
```

---

**После исправления все заработает! 🎉**
