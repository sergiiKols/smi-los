# 🔧 Решение: Удалите API_BASE_URL

## Проблема

При нажатии [Redeploy] Build падает с ошибкой:
```
Environment Variable "API_BASE_URL" references Secret "api_base_url", 
which does not exist.
```

И диалог Redeploy открывается снова → бесконечный цикл.

---

## Причина

Переменная `API_BASE_URL` неправильно настроена в системе Vercel, и даже после пересоздания как Plaintext проблема не исчезла.

**Вероятная причина:** Баг в Vercel, где старая конфигурация (Secret Reference) застряла в системе.

---

## ✅ Решение: Удалите переменную

### Пошаговая инструкция:

#### Шаг 1: Отмените Redeploy

Нажмите **[Cancel]** в диалоге Redeploy.

#### Шаг 2: Откройте Environment Variables

**Settings → Environment Variables**

#### Шаг 3: Найдите API_BASE_URL

Должна быть переменная:
```
API_BASE_URL
🚫 http://localhost:3001
All Environments
Added 17m ago
```

#### Шаг 4: Удалите переменную

1. Нажмите **⋯** (три точки справа от переменной)
2. Выберите **Delete** или **Remove**
3. Подтвердите удаление

#### Шаг 5: Убедитесь что переменная удалена

После удаления должны остаться только 4 переменные:
- ✅ NEXTAUTH_URL
- ✅ NEXTAUTH_SECRET
- ✅ ADMIN_USERNAME
- ✅ ADMIN_PASSWORD

#### Шаг 6: Запустите Redeploy

1. Перейдите на: **Deployments**
2. Нажмите: **[Redeploy]**
3. Теперь диалог НЕ должен показывать ошибку!
4. Нажмите: **[Redeploy]** (второй раз)
5. Дождитесь завершения Build (1-2 минуты)

---

## Почему это работает

### API_BASE_URL не обязательна

Эта переменная используется только для подключения Frontend к Backend API.

**Сейчас:**
- Backend НЕ развернут
- API работает локально на localhost:3001
- Frontend не использует эту переменную

**Поэтому:**
- Приложение будет работать БЕЗ API_BASE_URL
- Страница входа загрузится
- Авторизация работает через NextAuth (не зависит от API)
- Dashboard откроется (статистика будет пустая, но это нормально)

### Когда понадобится API_BASE_URL

После развертывания Backend на Railway/Render:

1. Получите URL Backend: `https://your-backend.railway.app`
2. Добавьте переменную обратно:
   ```
   Name: API_BASE_URL
   Type: Plaintext
   Value: https://your-backend.railway.app
   ```
3. Redeploy

---

## После удаления и Redeploy

### Что должно произойти:

1. ✅ Build пройдет успешно (без ошибок)
2. ✅ Deploy завершится со статусом "Ready"
3. ✅ Откройте сайт: https://smi-los1.vercel.app
4. ✅ Должна появиться **страница входа**!

### Проверка:

**Откройте сайт:**
```
https://smi-los1.vercel.app
```

**Должно появиться:**
- 🔒 Иконка замка
- 📝 Форма входа
- 🎨 Красивый дизайн с градиентом
- 💡 Подсказка с паролями по умолчанию

**Войдите:**
- Логин: `admin`
- Пароль: `admin123`

**Результат:**
- ✅ Dashboard откроется
- ⚠️ Статистика будет пустая (0/0/0) - это нормально, Backend не развернут
- ✅ Навигация работает (Dashboard, Статьи, Настройки)
- ✅ Кнопка "Выход" работает

---

## Если все еще не работает

### Вариант 1: Удалите ВСЕ переменные окружения

Радикальный метод:

1. Settings → Environment Variables
2. Удалите **ВСЕ** 5 переменных
3. Deployments → Redeploy
4. После успешного деплоя добавьте только 4 переменные обратно:
   - NEXTAUTH_URL
   - NEXTAUTH_SECRET
   - ADMIN_USERNAME
   - ADMIN_PASSWORD
5. Redeploy еще раз

### Вариант 2: Переподключите Git

Полностью очистит всю конфигурацию:

1. Settings → Git → Disconnect
2. Подтвердите отключение
3. Settings → Git → Connect Git Repository
4. Выберите sergiiKols/smi-los
5. Production Branch: main
6. Deployments → Redeploy

### Вариант 3: Пересоздайте проект (крайний случай)

Если ничего не помогает:

1. Создайте новый проект на Vercel
2. Import Git Repository: sergiiKols/smi-los
3. Root Directory: web
4. Добавьте 4 переменные (БЕЗ API_BASE_URL)
5. Deploy

---

## Проверка логов

Если Build упал с другой ошибкой:

1. **Deployments** → Нажмите на упавший деплой
2. Откройте **Build Logs**
3. Найдите красную строку с ошибкой
4. Скопируйте текст ошибки

Пришлите текст ошибки - разберемся!

---

## Итог

**Удалите API_BASE_URL и все заработает!**

Эта переменная нужна только для Backend, которого пока нет. После развертывания Backend добавим её обратно с правильным URL.

---

**Удачи с деплоем!** 🚀
