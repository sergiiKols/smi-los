# 🔄 Как работает автодеплой Vercel

## Вопрос: Почему Vercel не подгружает автоматически с GitHub?

**Ответ:** Vercel **ПОДГРУЖАЕТ автоматически**! Просто есть нюанс.

---

## ✅ Что работает

На вашем проекте автодеплой **УЖЕ РАБОТАЕТ**:

1. ✅ В логах видно: "Automatically created for pushes to sergiiKols/smi-los1"
2. ✅ Множество автоматических деплоев (каждые несколько минут)
3. ✅ Последний деплой запустился автоматически 12 минут назад

---

## ❌ Но почему все еще 404?

### Причина:

**Root Directory был изменен ПОСЛЕ последнего автодеплоя!**

### Временная линия:

```
12:00 - git push → Автодеплой с Root Directory: "" (пусто)
       ↓
12:05 - ВЫ изменили Root Directory на "web" в настройках
       ↓
12:10 - git push → Нет изменений в коде, автодеплой не запустился
       ↓
Сейчас - 404 ошибка, потому что последний деплой был БЕЗ Root Directory
```

---

## 🔧 Решение

### Вариант 1: Сохраните настройки (рекомендуется)

1. **Settings → General**
2. **Root Directory: web**
3. **Нажмите [Save]**
4. Vercel автоматически запустит новый деплой с правильными настройками!

### Вариант 2: Ручной Redeploy

1. **Deployments → [Redeploy]**
2. Подождите завершения
3. Новый деплой использует текущие настройки (Root Directory: web)

---

## 📊 Как работает автодеплой на Vercel

### Шаг 1: Git Push

```bash
git push
```

### Шаг 2: GitHub Webhook

GitHub отправляет уведомление на Vercel:
```
POST https://vercel.com/api/webhooks/github
{
  "repository": "sergiiKols/smi-los",
  "branch": "main",
  "commit": "abc123..."
}
```

### Шаг 3: Vercel Build

Vercel запускает сборку с **ТЕКУЩИМИ настройками**:
- ✅ Root Directory (из Settings)
- ✅ Environment Variables (из Settings)
- ✅ Build Command
- ✅ Framework Preset

### Шаг 4: Deploy

Если сборка успешна → деплой на продакшн
Если ошибка → деплой не происходит

---

## 🎯 Когда автодеплой НЕ запускается

### 1. Нет изменений в коде

```bash
git push
# Output: Everything up-to-date
```

Vercel не получает webhook от GitHub.

**Решение:** Сделайте любое изменение или ручной Redeploy.

### 2. Деплой заблокирован настройками

Settings → Git → Автодеплой выключен

**Решение:** Включите "Automatically deploy"

### 3. Build Cache

Vercel использует кэш и пропускает деплой.

**Решение:** Redeploy с "Clear cache"

---

## ✅ Проверка что автодеплой работает

### 1. Проверьте настройки Git

**Settings → Git**

Должно быть:
```
✅ Connected to GitHub
   Repository: sergiiKols/smi-los
   Production Branch: main

☑️ Automatically deploy main branch
☑️ Automatically deploy preview branches
```

### 2. Сделайте тестовый commit

```bash
echo "test" >> README.md
git add README.md
git commit -m "Test autodeploy"
git push
```

### 3. Проверьте Deployments

Должен появиться новый деплой со статусом "Building"

---

## 🔄 После сохранения настроек

После того как вы сохраните **Root Directory: web**:

### Что происходит:

1. ✅ Vercel автоматически запускает новый деплой
2. ✅ Использует Root Directory: web
3. ✅ Находит Next.js приложение в папке web/
4. ✅ Успешно собирает проект
5. ✅ Деплоит на продакшн
6. ✅ 404 исчезает!

### В будущем:

При каждом `git push`:
1. ✅ GitHub → webhook → Vercel
2. ✅ Vercel получает код из ветки main
3. ✅ Запускает Build с Root Directory: web
4. ✅ Деплоит автоматически
5. ✅ Сайт обновляется!

---

## 🐛 Troubleshooting

### Автодеплой не работает?

**Проверьте:**

1. **GitHub Webhooks**
   - Зайдите на GitHub: Repository → Settings → Webhooks
   - Должен быть webhook от Vercel
   - Статус: ✅ Recent Deliveries должны быть успешными

2. **Vercel Settings → Git**
   - ☑️ Automatically deploy должно быть включено
   - Connected repository должен быть правильным

3. **Branch**
   - Убедитесь что пушите в правильную ветку (main)
   - Production Branch в настройках = main

### Webhook отсутствует?

**Решение:** Переподключите репозиторий

1. Settings → Git → Disconnect
2. Settings → Git → Connect Git Repository
3. Выберите sergiiKols/smi-los
4. Webhook создастся автоматически

---

## 💡 Лучшие практики

### 1. Всегда сохраняйте настройки

После изменения любых настроек (Root Directory, Environment Variables):
- ✅ Нажмите [Save]
- ✅ Дождитесь автоматического деплоя
- ✅ Проверьте результат

### 2. Используйте Preview Deployments

Каждый Pull Request автоматически получает preview URL для тестирования.

### 3. Проверяйте логи

Deployments → Нажмите на деплой → Build Logs

Здесь видны все ошибки и предупреждения.

### 4. Environment Variables

Добавляйте переменные ДО первого деплоя, чтобы избежать ошибок.

---

## 📚 Полезные ссылки

- Vercel Docs: https://vercel.com/docs
- Automatic Deployments: https://vercel.com/docs/deployments/automatic-deployments
- GitHub Integration: https://vercel.com/docs/deployments/git/vercel-for-github

---

## ✅ Итог

**Автодеплой работает!** Просто нужно:

1. ✅ Сохранить Root Directory: web
2. ✅ Добавить Environment Variables
3. ✅ Подождать автоматический деплой ИЛИ нажать Redeploy

После этого каждый `git push` будет автоматически деплоиться! 🚀
