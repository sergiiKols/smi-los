# Система автоматического поиска и публикации статей

Автоматизированная система для поиска, анализа и публикации статей на сайте https://energo-audit.by/ и в социальных сетях (Facebook, Instagram).

## 🎯 Возможности

1. **Ежедневный поиск статей** - использует Google Gemini API для поиска релевантных статей по заданным ключевым словам
2. **AI-анализ качества** - автоматическая оценка статей по релевантности, качеству и бизнес-ценности
3. **Публикация в WordPress** - автоматическое создание постов на блоге
4. **Публикация в соцсети** - автоматический постинг в Facebook и Instagram
5. **Планировщик задач** - настраиваемое расписание для всех операций

## 📋 Требования

- Python 3.8+
- WordPress сайт с REST API
- API ключи:
  - Google Gemini API
  - WordPress (username + app password)
  - Facebook Graph API
  - Instagram Graph API

## 🚀 Установка

### 1. Клонируйте проект

```bash
git clone <repository-url>
cd content-search-system
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Настройте конфигурацию

Скопируйте файл `.env.example` в `.env`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и добавьте свои API ключи:

```env
GEMINI_API_KEY=ваш_ключ_gemini
WORDPRESS_URL=https://energo-audit.by
WORDPRESS_USERNAME=ваш_логин
WORDPRESS_PASSWORD=ваш_app_password
FACEBOOK_ACCESS_TOKEN=ваш_токен_facebook
FACEBOOK_PAGE_ID=id_страницы
INSTAGRAM_ACCESS_TOKEN=ваш_токен_instagram
INSTAGRAM_BUSINESS_ACCOUNT_ID=id_бизнес_аккаунта
```

### 4. Создайте необходимые директории

```bash
mkdir -p data logs
```

## 💻 Использование

### Режим планировщика (основной)

Запустите систему в режиме планировщика для автоматической работы:

```bash
python main.py --mode scheduler
```

Система будет выполнять задачи по расписанию:
- **09:00** - Поиск и сбор статей
- **10:00** - Публикация на блоге
- **12:00** - Публикация в Facebook
- **14:00** - Публикация в Instagram

### Ручной запуск задач

#### Только поиск статей
```bash
python main.py --mode search
```

#### Только публикация в блог
```bash
python main.py --mode publish-blog
```

#### Только публикация в соцсети
```bash
python main.py --mode publish-social
```

### Тестовый режим

Проверьте работу поиска с конкретным ключевым словом:

```bash
python main.py --mode test --test-keyword "тепловизор для обследования дома"
```

## 🔧 Настройка

### Расписание

Измените время выполнения задач в `.env`:

```env
SEARCH_HOUR=9
SEARCH_MINUTE=0
BLOG_POST_HOUR=10
BLOG_POST_MINUTE=0
FACEBOOK_POST_HOUR=12
FACEBOOK_POST_MINUTE=0
INSTAGRAM_POST_HOUR=14
INSTAGRAM_POST_MINUTE=0
```

### Ключевые слова

Отредактируйте список ключевых слов в `config.py`:

```python
KEYWORDS = [
    "расчет воздухообмена в помещении",
    "тепловизор для обследования дома",
    # ... добавьте свои ключевые слова
]
```

### Критерии отбора статей

Настройте минимальный балл для публикации:

```env
MIN_ARTICLE_SCORE=7.0
MAX_ARTICLES_PER_DAY=5
```

## 📁 Структура проекта

```
.
├── main.py                      # Главный файл запуска
├── config.py                    # Конфигурация
├── database.py                  # Работа с базой данных
├── gemini_search.py            # Поиск и анализ через Gemini
├── wordpress_publisher.py      # Публикация в WordPress
├── social_media_publisher.py   # Публикация в соцсети
├── scheduler.py                # Планировщик задач
├── requirements.txt            # Зависимости Python
├── .env.example               # Пример конфигурации
├── project_config.md          # Документация проекта
├── README.md                  # Этот файл
├── data/                      # База данных SQLite
└── logs/                      # Логи работы системы
```

## 🗄️ База данных

Система использует SQLite для хранения:
- Найденных статей
- Истории публикаций
- Истории поиска

База создается автоматически при первом запуске.

### Таблицы

- `articles` - найденные и проанализированные статьи
- `publications` - история публикаций на разных платформах
- `search_history` - история поиска по ключевым словам

## 🔑 Получение API ключей

### Google Gemini API

1. Перейдите на https://makersuite.google.com/app/apikey
2. Создайте новый API ключ
3. Скопируйте ключ в `.env`

### WordPress

1. Войдите в WordPress админку
2. Перейдите в "Пользователи" → "Профиль"
3. Прокрутите до "Пароли приложений"
4. Создайте новый пароль приложения
5. Используйте его как `WORDPRESS_PASSWORD`

### Facebook/Instagram

1. Перейдите на https://developers.facebook.com/
2. Создайте приложение
3. Настройте доступы к Facebook Pages и Instagram Graph API
4. Получите токены доступа и ID страниц

Подробная инструкция: https://developers.facebook.com/docs/pages/access-tokens

## 🐛 Отладка

Логи сохраняются в консоли. Для сохранения в файл:

```bash
python main.py --mode scheduler 2>&1 | tee logs/system.log
```

## ⚠️ Важные замечания

1. **Instagram требует изображения** - для публикации в Instagram обязательно нужны изображения
2. **WordPress статус** - по умолчанию посты создаются как "черновик" (`draft`). Измените на `publish` в `scheduler.py` для автопубликации
3. **Rate Limits** - учитывайте лимиты API при настройке частоты запросов
4. **Безопасность** - никогда не коммитьте файл `.env` с реальными ключами

## 📈 Мониторинг

Проверяйте базу данных для анализа работы системы:

```bash
sqlite3 data/articles.db "SELECT COUNT(*) FROM articles;"
sqlite3 data/articles.db "SELECT platform, COUNT(*) FROM publications GROUP BY platform;"
```

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи
2. Убедитесь, что все API ключи настроены правильно
3. Проверьте подключение к интернету
4. Убедитесь, что WordPress REST API доступен

## 📄 Лицензия

Все права защищены © energo-audit.by

## 🔄 Обновления

Для обновления зависимостей:

```bash
pip install --upgrade -r requirements.txt
```

## 🚦 Автозапуск при старте системы

### Linux (systemd)

Создайте файл `/etc/systemd/system/content-search.service`:

```ini
[Unit]
Description=Content Search and Publishing System
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/content-search-system
ExecStart=/usr/bin/python3 /path/to/content-search-system/main.py --mode scheduler
Restart=always

[Install]
WantedBy=multi-user.target
```

Активируйте сервис:

```bash
sudo systemctl enable content-search
sudo systemctl start content-search
```

### Windows

Используйте планировщик задач Windows или создайте службу с помощью NSSM.

---

**Создано для energo-audit.by** - профессиональный энергоаудит и тепловизионное обследование
