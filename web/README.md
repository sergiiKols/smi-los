# Web Dashboard для Content Search System

Современный веб-интерфейс для управления системой автоматического поиска и публикации контента.

## 🚀 Технологии

- **Next.js 14** - React фреймворк
- **TypeScript** - типизация
- **Tailwind CSS** - стилизация
- **Lucide React** - иконки
- **React Toastify** - уведомления
- **SWR** - кеширование данных

## 📦 Установка

### Локальная разработка

1. Установите зависимости:
```bash
cd web
npm install
```

2. Создайте файл `.env.local`:
```bash
cp .env.example .env.local
```

3. Отредактируйте `.env.local`:
```env
API_BASE_URL=http://localhost:3001
```

4. Запустите сервер разработки:
```bash
npm run dev
```

Приложение будет доступно на http://localhost:3000

## 🌐 Деплой на Vercel

### Способ 1: Через Git (рекомендуется)

1. **Создайте репозиторий на GitHub**
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/content-search-system.git
git push -u origin main
```

2. **Подключите к Vercel**
   - Зайдите на https://vercel.com
   - Нажмите "New Project"
   - Импортируйте ваш репозиторий
   - Выберите директорию `web` как Root Directory
   - Framework Preset: Next.js (автоматически)

3. **Настройте переменные окружения**

В настройках проекта на Vercel добавьте:
```
API_BASE_URL=https://your-api-server.com
```

4. **Деплой**
   - Vercel автоматически деплоит при push в main
   - Или нажмите "Deploy" в панели управления

### Способ 2: Через Vercel CLI

1. Установите Vercel CLI:
```bash
npm install -g vercel
```

2. Залогиньтесь:
```bash
vercel login
```

3. Деплой:
```bash
cd web
vercel
```

4. Для продакшн деплоя:
```bash
vercel --prod
```

## 🔧 Настройка API Backend

Backend API должен быть развернут отдельно. Опции:

### Вариант 1: Railway / Render

1. Создайте новый проект
2. Подключите GitHub репозиторий
3. Установите Python и зависимости из `api_requirements.txt`
4. Запустите: `python api_server.py`
5. Используйте полученный URL в `API_BASE_URL`

### Вариант 2: Собственный сервер

1. Установите зависимости:
```bash
pip install -r api_requirements.txt
```

2. Запустите API сервер:
```bash
python api_server.py
```

3. Сервер будет доступен на http://localhost:3001

### Вариант 3: Docker

```dockerfile
FROM python:3.9
WORKDIR /app
COPY . .
RUN pip install -r api_requirements.txt
CMD ["python", "api_server.py"]
```

## 📱 Возможности Dashboard

### Главная страница (Dashboard)
- 📊 Статистика в реальном времени
- 📈 Последние найденные статьи
- 📝 Лог активности
- ⚡ Быстрые действия (поиск, публикация)

### Страница статей
- 📋 Список всех найденных статей
- 🔍 Фильтрация по статусу
- ⭐ Просмотр балла AI и релевантности
- ✅ Одобрение/отклонение статей
- 👁️ Детальный просмотр

### Страница настроек
- ⏰ Настройка расписания для всех задач
- 📊 Параметры контента (мин. балл, макс. статей)
- 💾 Сохранение конфигурации

## 🎨 Кастомизация

### Цветовая схема

Отредактируйте `tailwind.config.js`:

```js
theme: {
  extend: {
    colors: {
      primary: {
        // Ваши цвета
      },
    },
  },
}
```

### Логотип и брендинг

Замените в `app/page.tsx`:
- Иконку TrendingUp на ваш логотип
- Название "Content Dashboard"

## 🔒 Аутентификация (опционально)

Для добавления аутентификации используйте NextAuth.js:

1. Установите:
```bash
npm install next-auth
```

2. Создайте `app/api/auth/[...nextauth]/route.ts`

3. Добавьте провайдеры (Google, GitHub, Email)

## 🐛 Отладка

### Проблема: API не отвечает

Проверьте:
- API сервер запущен (`python api_server.py`)
- Правильный URL в `.env.local`
- CORS настроен в `api_server.py`

### Проблема: Стили не работают

```bash
npm run dev
# или
rm -rf .next
npm run build
```

### Проблема: TypeScript ошибки

```bash
npm run lint
```

## 📊 Мониторинг в продакшене

На Vercel доступны:
- Analytics (посещаемость)
- Speed Insights (производительность)
- Логи деплоев
- Real-time функции

## 🔄 Обновление

```bash
git pull origin main
npm install
npm run build
```

На Vercel обновление происходит автоматически при push.

## 📝 Структура проекта

```
web/
├── app/
│   ├── layout.tsx          # Общий layout
│   ├── page.tsx            # Главная страница (Dashboard)
│   ├── globals.css         # Глобальные стили
│   ├── articles/
│   │   └── page.tsx        # Страница статей
│   └── settings/
│       └── page.tsx        # Страница настроек
├── components/
│   ├── DashboardStats.tsx  # Статистика
│   ├── RecentArticles.tsx  # Последние статьи
│   └── ActivityLog.tsx     # Лог активности
├── public/                 # Статические файлы
├── package.json
├── next.config.js
├── tailwind.config.js
└── tsconfig.json
```

## 🌟 Будущие улучшения

- [ ] Аутентификация пользователей
- [ ] Редактирование статей в интерфейсе
- [ ] Предпросмотр постов перед публикацией
- [ ] Графики и аналитика
- [ ] Темная/светлая тема
- [ ] Мобильная оптимизация
- [ ] Уведомления в реальном времени (WebSocket)

## 🤝 Поддержка

Проблемы? Создайте issue на GitHub или свяжитесь с разработчиком.

---

**Создано для energo-audit.by** 🚀
