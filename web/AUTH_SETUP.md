# 🔐 Настройка аутентификации

Система защищена логином и паролем с использованием NextAuth.js.

---

## 🎯 Что добавлено

✅ Страница входа (`/login`)  
✅ Защита всех страниц админки  
✅ Кнопка выхода  
✅ Middleware для проверки аутентификации  
✅ Сессии на 30 дней  

---

## 🔑 Учетные данные по умолчанию

```
Логин: admin
Пароль: admin123
```

⚠️ **ВАЖНО:** Измените эти данные в продакшене!

---

## ⚙️ Настройка на Vercel

### Шаг 1: Добавьте переменные окружения

Зайдите в настройки проекта на Vercel:
**Settings → Environment Variables**

Добавьте следующие переменные:

```
NEXTAUTH_URL=https://ваш-домен.vercel.app
NEXTAUTH_SECRET=случайная-длинная-строка-минимум-32-символа
ADMIN_USERNAME=ваш_логин
ADMIN_PASSWORD=ваш_надежный_пароль
```

### Шаг 2: Сгенерируйте NEXTAUTH_SECRET

**Способ 1: Online Generator**
https://generate-secret.vercel.app/32

**Способ 2: OpenSSL**
```bash
openssl rand -base64 32
```

**Способ 3: Node.js**
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

### Шаг 3: Redeploy проекта

После добавления переменных нажмите **Redeploy** на Vercel.

---

## 🌐 Локальная разработка

### Шаг 1: Создайте файл .env.local

В папке `web/` создайте `.env.local`:

```bash
cd web
cp .env.example .env.local
```

### Шаг 2: Отредактируйте .env.local

```env
# API Configuration
API_BASE_URL=http://localhost:3001

# Authentication
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-random-secret-key-min-32-chars

# Admin credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
```

### Шаг 3: Установите зависимости

```bash
npm install
```

### Шаг 4: Запустите

```bash
npm run dev
```

Откройте http://localhost:3000 - вас перенаправит на `/login`

---

## 🔒 Как это работает

### 1. Middleware защищает все страницы

Файл `middleware.ts` проверяет аутентификацию на всех страницах кроме:
- `/login` - страница входа
- `/api/auth/*` - NextAuth endpoints
- Статические файлы

### 2. Страница входа

`/app/login/page.tsx` - красивая форма входа с:
- Полем логина
- Полем пароля
- Обработкой ошибок
- Индикатором загрузки

### 3. NextAuth API

`/app/api/auth/[...nextauth]/route.ts` - обрабатывает:
- Вход (sign in)
- Выход (sign out)
- Проверку сессии
- JWT токены

### 4. Провайдер аутентификации

`/components/AuthProvider.tsx` - оборачивает приложение в SessionProvider

### 5. Кнопка выхода

`/components/LogoutButton.tsx` - кнопка выхода в навигации

---

## 🎨 Настройка внешнего вида

### Изменить логотип на странице входа

Отредактируйте `web/app/login/page.tsx`:

```tsx
<div className="inline-flex items-center justify-center w-16 h-16 bg-primary-600 rounded-full mb-4">
  <Lock className="h-8 w-8 text-white" />
  {/* Замените на ваш логотип */}
</div>
```

### Изменить заголовок

```tsx
<h1 className="text-3xl font-bold text-gray-900 dark:text-white mb-2">
  Ваше название
</h1>
```

---

## 👥 Добавление нескольких пользователей

Для добавления нескольких пользователей отредактируйте:

`web/app/api/auth/[...nextauth]/route.ts`

```typescript
async authorize(credentials) {
  // Список пользователей
  const users = [
    { username: 'admin', password: 'admin123', name: 'Администратор' },
    { username: 'manager', password: 'manager123', name: 'Менеджер' },
    { username: 'editor', password: 'editor123', name: 'Редактор' },
  ]

  const user = users.find(
    u => u.username === credentials?.username && 
         u.password === credentials?.password
  )

  if (user) {
    return {
      id: user.username,
      name: user.name,
      email: user.username + '@admin.local',
    }
  }
  
  return null
}
```

⚠️ **Для продакшена используйте базу данных и хэширование паролей!**

---

## 🔐 Безопасность в продакшене

### 1. Используйте надежные пароли

```
❌ Плохо: admin, 123456, password
✅ Хорошо: K8$mP9#nL2@qR5!vX7
```

### 2. Храните пароли хэшированными

Используйте bcrypt:

```bash
npm install bcrypt
```

```typescript
import bcrypt from 'bcrypt'

// При регистрации
const hashedPassword = await bcrypt.hash(password, 10)

// При входе
const isValid = await bcrypt.compare(credentials.password, hashedPassword)
```

### 3. Используйте HTTPS

Vercel автоматически предоставляет HTTPS.

### 4. Ограничьте попытки входа

Добавьте rate limiting для предотвращения brute-force атак.

### 5. Двухфакторная аутентификация

Рассмотрите добавление 2FA для дополнительной безопасности.

---

## 🔧 Расширенные настройки

### Изменить время жизни сессии

В `route.ts`:

```typescript
session: {
  strategy: 'jwt',
  maxAge: 7 * 24 * 60 * 60, // 7 дней (вместо 30)
},
```

### Добавить роли пользователей

```typescript
async jwt({ token, user }) {
  if (user) {
    token.id = user.id
    token.role = 'admin' // добавьте роль
  }
  return token
}
```

### Перенаправление после входа

В `login/page.tsx`:

```typescript
if (result?.ok) {
  router.push('/dashboard') // вместо '/'
}
```

---

## 🧪 Тестирование

### Проверьте защиту страниц

1. Откройте сайт в режиме инкогнито
2. Попробуйте открыть `/` или `/articles`
3. Должна открыться страница `/login`

### Проверьте вход

1. Введите правильные учетные данные
2. Должен открыться Dashboard
3. В навигации должна быть кнопка "Выход"

### Проверьте выход

1. Нажмите кнопку "Выход"
2. Должна открыться страница `/login`
3. При попытке вернуться назад - снова `/login`

---

## 🐛 Решение проблем

### Ошибка: "NEXTAUTH_SECRET not set"

Добавьте переменную `NEXTAUTH_SECRET` в `.env.local` или на Vercel.

### Ошибка: "Invalid credentials"

Проверьте что `ADMIN_USERNAME` и `ADMIN_PASSWORD` совпадают с введенными.

### Постоянно перенаправляет на /login

1. Очистите cookies браузера
2. Проверьте что `NEXTAUTH_URL` правильный
3. Проверьте что `NEXTAUTH_SECRET` установлен

### Ошибка: "Module not found: next-auth"

Выполните:
```bash
cd web
npm install
```

---

## 📚 Дополнительно

### Документация NextAuth.js
https://next-auth.js.org/

### Примеры
https://github.com/nextauthjs/next-auth-example

### Провайдеры
NextAuth поддерживает множество провайдеров:
- Google OAuth
- GitHub OAuth
- Facebook OAuth
- Email (Magic Links)
- И многие другие

---

## ✅ Чеклист готовности

- [ ] Изменен `ADMIN_USERNAME` на Vercel
- [ ] Изменен `ADMIN_PASSWORD` на Vercel
- [ ] Сгенерирован `NEXTAUTH_SECRET`
- [ ] Проверен вход/выход локально
- [ ] Проверена защита всех страниц
- [ ] Удалена подсказка с паролями по умолчанию (строка 88 в login/page.tsx)

---

**Система аутентификации готова! 🎉**
