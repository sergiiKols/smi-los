# 🔧 Исправление ошибки Environment Variable

## Ошибка

```
Environment Variable "API_BASE_URL" references Secret "api_base_url", 
which does not exist.
```

---

## Причина

При добавлении переменной `API_BASE_URL` был выбран неправильный тип:
- ❌ **Secret Reference** - ссылка на несуществующий секрет
- ✅ Должно быть: **Plaintext** - прямое значение

---

## Решение

### Шаг 1: Удалите неправильную переменную

1. **Settings → Environment Variables**
2. Найдите **API_BASE_URL**
3. Нажмите **⋯** (три точки) → **Delete**
4. Подтвердите удаление

### Шаг 2: Добавьте переменную правильно

1. Нажмите **[Add New]**

2. Заполните форму:

```
┌─────────────────────────────────────────────────┐
│ Key:                                            │
│ API_BASE_URL                                    │
│                                                 │
│ Value Type: Plaintext  ← ВАЖНО!                │
│                                                 │
│ Value:                                          │
│ http://localhost:3001                           │
│                                                 │
│ Environments:                                   │
│ ✅ Production                                   │
│ ✅ Preview                                      │
│ ✅ Development                                  │
└─────────────────────────────────────────────────┘
```

3. Нажмите **[Save]**

---

## Типы переменных в Vercel

### Plaintext

Обычное значение, которое будет доступно в коде.

**Использовать для:**
- URLs
- Публичных ключей
- Настроек

**Пример:**
```
API_BASE_URL = http://localhost:3001
NEXTAUTH_URL = https://smi-los1.vercel.app
```

### Secret Reference

Ссылка на секрет, созданный в разделе **Secrets**.

**Использовать для:**
- Приватных ключей
- Токенов
- Паролей (опционально)

**Как использовать:**
1. Сначала создайте Secret: **Settings → Secrets → [Add Secret]**
2. Потом создайте переменную со ссылкой на этот Secret

---

## Все переменные для проекта

Вот ВСЕ переменные, которые нужно добавить:

### 1. NEXTAUTH_URL
```
Type: Plaintext
Value: https://smi-los1.vercel.app
```

### 2. NEXTAUTH_SECRET
```
Type: Plaintext (или Secret)
Value: <сгенерируйте на https://generate-secret.vercel.app/32>
```

### 3. ADMIN_USERNAME
```
Type: Plaintext
Value: admin
```

### 4. ADMIN_PASSWORD
```
Type: Plaintext (или Secret для безопасности)
Value: admin123
```

### 5. API_BASE_URL
```
Type: Plaintext ← ВАЖНО!
Value: http://localhost:3001
```

---

## После исправления

1. Все переменные должны быть добавлены
2. Vercel автоматически запустит redeploy
3. ИЛИ запустите вручную: **Deployments → [Redeploy]**
4. Дождитесь статуса **"Ready"**
5. Откройте сайт - должна появиться страница входа!

---

## Проверка переменных

После добавления всех переменных проверьте:

**Settings → Environment Variables**

Должно быть 5 переменных, все типа **Plaintext** (или Secret для чувствительных данных):

```
✅ NEXTAUTH_URL          → https://smi-los1.vercel.app
✅ NEXTAUTH_SECRET       → aB3dE7fG9h...
✅ ADMIN_USERNAME        → admin
✅ ADMIN_PASSWORD        → admin123
✅ API_BASE_URL          → http://localhost:3001
```

---

## Что делать если ошибка повторяется

### 1. Проверьте тип переменной

При создании убедитесь что выбран **Plaintext**, а не **Secret Reference**.

### 2. Очистите все переменные и добавьте заново

Удалите все 5 переменных и добавьте по очереди, внимательно выбирая тип.

### 3. Проверьте логи деплоя

**Deployments → Нажмите на деплой → Build Logs**

Ищите строки с упоминанием переменных окружения.

---

**После исправления все заработает!** 🚀
